import 'package:flutter/material.dart';

class ResulteView extends StatelessWidget {
  const ResulteView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
